/**
 * Central App Constants
 * Single source of truth for app-wide magic values.
 * Update here when releasing a new version.
 */

export const APP_VERSION = "v1.0.0";

/** Default session estimate (minutes) for users with no telemetry data */
export const DEFAULT_SESSION_MINUTES = 15;

/** Default theme for new users */
export const DEFAULT_THEME = "material-design";

/** App name used in legal/support contexts */
export const APP_NAME = "coZify";
